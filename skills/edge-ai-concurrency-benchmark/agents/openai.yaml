interface:
  display_name: "端侧多模型同驻与并发测量"
  short_description: "设计和分析端侧多模型冷启动、同驻、并发与资源基准，区分wall-clock、CPU时间、PSS、系统可用内存"
  default_prompt: "使用 $edge-ai-concurrency-benchmark，根据现有版本和证据完成本次任务，给出可复现结果与未验证边界。"
