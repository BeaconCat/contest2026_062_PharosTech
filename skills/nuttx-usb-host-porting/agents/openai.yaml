interface:
  display_name: "NuttX USB Host 与 Hub 移植"
  short_description: "将 SoC DWC3/xHCI 接入 NuttX USB Host，排查 PHY/VBUS、Hub 拓扑、设"
  default_prompt: "使用 $nuttx-usb-host-porting，根据现有版本和证据完成本次任务，给出可复现结果与未验证边界。"
